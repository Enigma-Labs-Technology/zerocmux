interface:
  display_name: "zerocmux Browser"
  short_description: "Discover and automate explicit zerocmux browser surfaces without changing focus."
  default_prompt: "Use this skill for zerocmux browser automation. Check `zerocmux browser --help`, discover the intended surface with read-only topology commands (including other workspaces), pass that surface to every existing-surface command, preserve visible focus, and re-snapshot after state changes."
