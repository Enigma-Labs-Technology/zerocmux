interface:
  display_name: "zerocmux Browser"
  short_description: "Automate zerocmux webview surfaces with snapshot/ref workflows."
  default_prompt: "Use this skill for browser automation via zerocmux CLI: identify target surface, snapshot interactive refs, perform actions, wait for state changes, and re-snapshot to avoid stale refs."
