interface:
  display_name: "zerocmux Customization"
  short_description: "Customize zerocmux actions, UI, layouts, and presets."
  default_prompt: "Use $zerocmux-customization to pick an examples-library preset, wire it into cmux.json, and validate the zerocmux config."
