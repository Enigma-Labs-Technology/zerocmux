interface:
  display_name: "zerocmux Diagnostics"
  short_description: "Check zerocmux hooks, restore, CLI, and settings."
  default_prompt: "Use $zerocmux-diagnostics to collect a read-only zerocmux health report and explain any hook or session restore problems."
