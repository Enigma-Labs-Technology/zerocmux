interface:
  display_name: "zerocmux Release"
  short_description: "Prepare zerocmux releases with version, changelog, guard, tag, and asset rules."
  default_prompt: "Use this skill when preparing a zerocmux release, bumping versions, updating CHANGELOG.md, running the release pretag guard, tagging, or troubleshooting release assets."
