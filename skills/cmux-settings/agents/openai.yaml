interface:
  display_name: "zerocmux Settings"
  short_description: "Inspect, edit, validate, and reload cmux.json settings."
  default_prompt: "Use this skill to change zerocmux settings safely: map user-facing preferences to cmux.json paths, edit with the bundled helper, validate recognized keys, and rely on zerocmux's live config reload."
